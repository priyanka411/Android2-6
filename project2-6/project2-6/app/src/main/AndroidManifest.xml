<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidprojectgroup_26">

    <application
        android:name=".Session.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".UI.Splash"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.MainActivity" />
        <activity android:name=".UI.Login" />
        <activity android:name=".UI.Register" />
        <activity
            android:name=".UI.SelectedFoodCenter"
            android:theme="@style/NoToolbar" />
        <activity android:name=".UI.LeaveComment" />
        <activity
            android:name=".UI.Thanku"
            android:theme="@style/NoToolbar" />
        <activity android:name=".UI.SelectedFoodCenterVendor" />
        <activity android:name=".UI.GraphView"></activity>
    </application>

</manifest>